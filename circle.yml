machine:
  java:
    version: oraclejdk8
  environment:
    GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx2048m -XX:+HeapDumpOnOutOfMemoryError"'

general:
  artifacts:
    - "/home/ubuntu/android-hospital/app/build/reports/checkstyle/checkstyle.html"

dependencies:
  pre:
    - chmod +x gradlew
    - if [ ! -d "/usr/local/android-sdk-linux/platforms/android-25" ]; then echo y | android update sdk --no-ui --all --filter "android-25"; fi
    - if [ ! -d "/usr/local/android-sdk-linux/build-tools/25.0.0" ]; then echo y | android update sdk --no-ui --all --filter "build-tools-25.0.5"; fi
  cache_directories:
    - ~/.android
    - ~/android

test:
  override:
    - ./gradlew check

#deployment:
#  master:
#    branch: master
#    commands:
#      - ./deploy_master.sh